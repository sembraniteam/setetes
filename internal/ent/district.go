// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/megalodev/setetes/internal/ent/district"
	"github.com/megalodev/setetes/internal/ent/subdistrict"
)

// District is the model entity for the District schema.
type District struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id"`
	// BpsCode holds the value of the "bps_code" field.
	BpsCode string `json:"bps_code"`
	// Name holds the value of the "name" field.
	Name string `json:"name"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DistrictQuery when eager-loading is set.
	Edges        DistrictEdges `json:"edges"`
	district_id  *uuid.UUID
	selectValues sql.SelectValues
}

// DistrictEdges holds the relations/edges for other nodes in the graph.
type DistrictEdges struct {
	// City holds the value of the city edge.
	City []*City `json:"city,omitempty"`
	// Subdistrict holds the value of the subdistrict edge.
	Subdistrict *Subdistrict `json:"subdistrict,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// CityOrErr returns the City value or an error if the edge
// was not loaded in eager-loading.
func (e DistrictEdges) CityOrErr() ([]*City, error) {
	if e.loadedTypes[0] {
		return e.City, nil
	}
	return nil, &NotLoadedError{edge: "city"}
}

// SubdistrictOrErr returns the Subdistrict value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e DistrictEdges) SubdistrictOrErr() (*Subdistrict, error) {
	if e.Subdistrict != nil {
		return e.Subdistrict, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: subdistrict.Label}
	}
	return nil, &NotLoadedError{edge: "subdistrict"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*District) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case district.FieldBpsCode, district.FieldName:
			values[i] = new(sql.NullString)
		case district.FieldID:
			values[i] = new(uuid.UUID)
		case district.ForeignKeys[0]: // district_id
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the District fields.
func (_m *District) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case district.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case district.FieldBpsCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field bps_code", values[i])
			} else if value.Valid {
				_m.BpsCode = value.String
			}
		case district.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case district.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field district_id", values[i])
			} else if value.Valid {
				_m.district_id = new(uuid.UUID)
				*_m.district_id = *value.S.(*uuid.UUID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the District.
// This includes values selected through modifiers, order, etc.
func (_m *District) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryCity queries the "city" edge of the District entity.
func (_m *District) QueryCity() *CityQuery {
	return NewDistrictClient(_m.config).QueryCity(_m)
}

// QuerySubdistrict queries the "subdistrict" edge of the District entity.
func (_m *District) QuerySubdistrict() *SubdistrictQuery {
	return NewDistrictClient(_m.config).QuerySubdistrict(_m)
}

// Update returns a builder for updating this District.
// Note that you need to call District.Unwrap() before calling this method if this District
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *District) Update() *DistrictUpdateOne {
	return NewDistrictClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the District entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *District) Unwrap() *District {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: District is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *District) String() string {
	var builder strings.Builder
	builder.WriteString("District(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("bps_code=")
	builder.WriteString(_m.BpsCode)
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteByte(')')
	return builder.String()
}

// Districts is a parsable slice of District.
type Districts []*District
