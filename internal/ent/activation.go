// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/megalodev/setetes/internal/ent/account"
	"github.com/megalodev/setetes/internal/ent/activation"
)

// Activation is the model entity for the Activation schema.
type Activation struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt int64 `json:"created_at"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt int64 `json:"updated_at"`
	// Represents soft delete timestamp in milliseconds.
	DeletedAt int64 `json:"deleted_at"`
	// The activation token is single-use and will be deleted after it is used.
	Token string `json:"-"`
	// IsUsed holds the value of the "is_used" field.
	IsUsed bool `json:"is_used"`
	// The activation link is only valid for 1 hour.
	ExpiredAt int64 `json:"expired_at"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ActivationQuery when eager-loading is set.
	Edges        ActivationEdges `json:"edges"`
	account_id   *uuid.UUID
	selectValues sql.SelectValues
}

// ActivationEdges holds the relations/edges for other nodes in the graph.
type ActivationEdges struct {
	// Account holds the value of the account edge.
	Account *Account `json:"account,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// AccountOrErr returns the Account value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ActivationEdges) AccountOrErr() (*Account, error) {
	if e.Account != nil {
		return e.Account, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: account.Label}
	}
	return nil, &NotLoadedError{edge: "account"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Activation) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case activation.FieldIsUsed:
			values[i] = new(sql.NullBool)
		case activation.FieldCreatedAt, activation.FieldUpdatedAt, activation.FieldDeletedAt, activation.FieldExpiredAt:
			values[i] = new(sql.NullInt64)
		case activation.FieldToken:
			values[i] = new(sql.NullString)
		case activation.FieldID:
			values[i] = new(uuid.UUID)
		case activation.ForeignKeys[0]: // account_id
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Activation fields.
func (_m *Activation) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case activation.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case activation.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Int64
			}
		case activation.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Int64
			}
		case activation.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				_m.DeletedAt = value.Int64
			}
		case activation.FieldToken:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field token", values[i])
			} else if value.Valid {
				_m.Token = value.String
			}
		case activation.FieldIsUsed:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_used", values[i])
			} else if value.Valid {
				_m.IsUsed = value.Bool
			}
		case activation.FieldExpiredAt:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field expired_at", values[i])
			} else if value.Valid {
				_m.ExpiredAt = value.Int64
			}
		case activation.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field account_id", values[i])
			} else if value.Valid {
				_m.account_id = new(uuid.UUID)
				*_m.account_id = *value.S.(*uuid.UUID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Activation.
// This includes values selected through modifiers, order, etc.
func (_m *Activation) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryAccount queries the "account" edge of the Activation entity.
func (_m *Activation) QueryAccount() *AccountQuery {
	return NewActivationClient(_m.config).QueryAccount(_m)
}

// Update returns a builder for updating this Activation.
// Note that you need to call Activation.Unwrap() before calling this method if this Activation
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Activation) Update() *ActivationUpdateOne {
	return NewActivationClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Activation entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Activation) Unwrap() *Activation {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Activation is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Activation) String() string {
	var builder strings.Builder
	builder.WriteString("Activation(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(fmt.Sprintf("%v", _m.CreatedAt))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(fmt.Sprintf("%v", _m.UpdatedAt))
	builder.WriteString(", ")
	builder.WriteString("deleted_at=")
	builder.WriteString(fmt.Sprintf("%v", _m.DeletedAt))
	builder.WriteString(", ")
	builder.WriteString("token=<sensitive>")
	builder.WriteString(", ")
	builder.WriteString("is_used=")
	builder.WriteString(fmt.Sprintf("%v", _m.IsUsed))
	builder.WriteString(", ")
	builder.WriteString("expired_at=")
	builder.WriteString(fmt.Sprintf("%v", _m.ExpiredAt))
	builder.WriteByte(')')
	return builder.String()
}

// Activations is a parsable slice of Activation.
type Activations []*Activation
